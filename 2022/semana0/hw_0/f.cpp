#include <bits/stdc++.h>

using namespace std;

int main(){
    int n, a, b;
    
    //NÃO MISTURAR CIN/COUT COM SCANF/PRINTF

    // cin >> a;
    // cout << (a + a * a + a * a * a)  << "\n";
    scanf("%d", &a);
    printf("%d \n", (a + a * a + a * a * a));
}