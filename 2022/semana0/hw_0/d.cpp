#include <bits/stdc++.h>

using namespace std;

int main(){
    int n, a, b;

    //NÃO MISTURAR CIN/COUT COM SCANF/PRINTF

    // cin >> n >> a >> b;
    // cout << (n - a + b) << "\n";
    scanf("%d %d %d", &n, &a, &b);
    printf("%d \n", (n - a + b));
}